import 'bootstrap';
// Bootstrap 5's default CSS is intentionally NOT imported here: the site
// still uses the legacy "toolkit"/theme stylesheets (assets/css/*.css),
// and loading Bootstrap 5's own CSS would conflict with that theme.
// Only the JS (collapse/modal/carousel/etc.) is bundled below.

// https://stackoverflow.com/a/53580347/185820
// import '@fortawesome/fontawesome-free/js/fontawesome'
// import '@fortawesome/fontawesome-free/js/solid'
// import '@fortawesome/fontawesome-free/js/regular'
// import '@fortawesome/fontawesome-free/js/brands'
